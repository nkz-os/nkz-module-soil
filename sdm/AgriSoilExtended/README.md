# AgriSoilExtended (SDM-001)

Proposal-grade extension of FIWARE `AgriSoil` for precision agriculture. See `notes.yaml` for rationale and `schema.json` for the formal contract.

## Status

`drafting` — internal review pending before upstream submission to `smart-data-models/dataModel.Agrifood`.

## Files

- `schema.json` — JSON Schema (draft-07).
- `example.json` — key-value example.
- `example-normalized.json` — NGSI-LD normalized example with provenance.
- `notes.yaml` — design rationale.
- `ADOPTERS.yaml` — production adopters.
- `context.jsonld` — JSON-LD context fragment with custom terms.
