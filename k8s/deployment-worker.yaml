apiVersion: apps/v1
kind: Deployment
metadata:
  name: soil-module-worker
  namespace: nekazari
spec:
  replicas: 1
  selector:
    matchLabels:
      app: soil-module-worker
  template:
    metadata:
      labels:
        app: soil-module-worker
    spec:
      containers:
        - name: worker
          image: ghcr.io/nkz-os/nkz-module-soil/soil-worker@sha256:b364c4adf3345743dfb6d644935f77fb5941d9cb1aa02a2d2d43b65b6fbfd618  # fix: +ArqRedis import on backfill
          command: ["arq", "nkz_soil.workers.ingest.WorkerSettings"]
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: soil-module-config
                optional: true
            - secretRef:
                name: soil-module-secret
                optional: false
          env:
            # pg-dsn has a hyphen, so envFrom secretRef cannot expose it as an env
            # var (invalid name) — map it explicitly. The worker cascade needs it.
            - name: SOIL_PG_DSN
              valueFrom:
                secretKeyRef:
                  name: soil-module-secret
                  key: pg-dsn
            - name: REDIS_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: redis-secret
                  key: password
            - name: REDIS_URL
              value: "redis://:$(REDIS_PASSWORD)@redis-service:6379/0"
            - name: ORION_LD_URL
              value: "http://orion-ld-service:1026"
            - name: ORION_BASE_URL
              value: "http://orion-ld-service:1026"
            - name: MINIO_ENDPOINT
              value: "http://minio-service:9000"
            - name: CONTEXT_URL
              value: "http://api-gateway-service:5000/ngsi-ld-context.json"
            - name: SOIL_DEFAULT_TENANT
              value: "platform"
          resources:
            requests:
              cpu: 100m
              memory: 256Mi
            limits:
              cpu: 1000m
              memory: 1Gi
          livenessProbe:
            exec:
              command:
                - sh
                - -c
                - "python -c \"from arq.connections import ArqRedis; import asyncio; asyncio.run(ArqRedis.from_url('${REDIS_URL}').info())\""
            initialDelaySeconds: 15
            periodSeconds: 30
